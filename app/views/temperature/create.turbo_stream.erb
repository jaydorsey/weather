<%= turbo_stream.update "temperature" do %>
  It's currently <%= @weather.temp %> <%= @weather.scale %> with a low of 
  <%= @weather.min_temp%> <%= @weather.scale %> and a high of
  <%= @weather.max_temp%> <%= @weather.scale %>. This temperature reading is
  <%= @cached ? "cached" : "not cached" %>
<% end %>
