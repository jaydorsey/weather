<%= turbo_stream.update "temperature" do %>
  In <%= zip_code.name %>, it's currently <%= weather.temp %> <%= weather.scale %> 
  with a low of <%= weather.min_temp%> <%= weather.scale %> 
  and a high of <%= weather.max_temp%> <%= weather.scale %>. 

  <% if cached %>
    <p>
      This temperature reading may be delayed by up to 30 minutes.
    </p>
  <% end %>
<% end %>
